dev:
  propel:
    class:                sfPropelDatabase
    param:
      phptype:            mysql     # Database vendor
      hostspec:           localhost
      database:           site_hindiworth
      username:           root
      password:           
#      port:               80
      encoding:           utf8      # Default charset for table creation
      persistent:         true      # Use persistent connections

hindiworth:
  propel:
    class:                sfPropelDatabase
    param:
      phptype:            mysql     # Database vendor
      hostspec:           localhost
      database:           hindiwor_Ashish
      username:          hindiwor_Ashish
      password:           Dj@AshishIN
#      port:               80
      encoding:           utf8      # Default charset for table creation
      persistent:         false      # Use persistent connections

                            
                            